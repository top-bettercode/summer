spring:
  cloud:
    config:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: config,env,refresh,health,loggers,logs
  endpoint:
    env:
      post:
        enabled: true